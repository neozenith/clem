# Diagrams

This folder is the central spot for MermaidJS diagrams (`.mmd`) and a `Makefile` to assist automating generating high resolution pngs when the source files update.

Simply:

```sh
make diagrams
```

from the root of the repo:

```sh
make -C docs/diagrams diagrams
```